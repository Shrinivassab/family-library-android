org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
# Disabled: file_picker 11.0.2 correctly skips the standalone Kotlin plugin under
# AGP 9+ (assuming built-in Kotlin), but camera_android_camerax's own buildscript
# still isn't fully AGP-9-clean, so builtInKotlin=true broke it. Pinned AGP itself
# back to a stable 8.x in settings.gradle.kts (matching what both plugins'
# buildscripts actually reference), which sidesteps the AGP9 transition entirely
# and lets both plugins apply their own standalone Kotlin plugin consistently.
android.builtInKotlin=false
